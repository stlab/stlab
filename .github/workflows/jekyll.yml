# Build Jekyll site + Doxygen API docs and deploy to GitHub Pages (stlab.cc).

name: Deploy Jekyll site to Pages

env:
  source: ./docs/

on:
  push:
    branches: [main]
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1.213.0
        with:
          ruby-version: '3.4.4'
          working-directory: docs
          bundler-cache: true
          cache-version: 0
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4
      - name: Install Doxygen
        uses: ssciwr/doxygen-install@501e53b879da7648ab392ee226f5b90e42148449
      - name: Install Ninja and Graphviz
        run: sudo apt-get update && sudo apt-get install -y ninja-build graphviz
      - name: Build full documentation site
        run: ./docs/tools/docs/build-site.sh --refresh-releases
        env:
          JEKYLL_BASEURL: ${{ steps.pages.outputs.base_path }}
          JEKYLL_ENV: production
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./docs/_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
